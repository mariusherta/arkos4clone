diff --git a/yabause/CMake/Packages/external_libchdr.cmake b/yabause/CMake/Packages/external_libchdr.cmake
index f6a8db60..0eb462cd 100644
--- a/yabause/CMake/Packages/external_libchdr.cmake
+++ b/yabause/CMake/Packages/external_libchdr.cmake
@@ -24,7 +24,11 @@ elseif(IOS)
   )
 
 else()
-  set(ADDITIONAL_CMAKE_ARGS "")
+  set(ADDITIONAL_CMAKE_ARGS
+    -DCMAKE_C_COMPILER=${CMAKE_C_COMPILER}
+    -DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER}
+    -DCMAKE_SYSROOT=${CMAKE_SYSROOT}
+  )
 endif()
 
 ExternalProject_Add(
diff --git a/yabause/CMake/Packages/external_libpng.cmake b/yabause/CMake/Packages/external_libpng.cmake
index eb73090a..6f6874e9 100644
--- a/yabause/CMake/Packages/external_libpng.cmake
+++ b/yabause/CMake/Packages/external_libpng.cmake
@@ -47,7 +47,13 @@ set( ADDITIONAL_CMAKE_ARGS
 )
 
 else()
-  set(ADDITIONAL_CMAKE_ARGS "")
+  set(ADDITIONAL_CMAKE_ARGS
+    -DCMAKE_C_COMPILER=${CMAKE_C_COMPILER}
+    -DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER}
+    -DCMAKE_SYSROOT=${CMAKE_SYSROOT}
+    -DPNG_HARDWARE_OPTIMIZATIONS=OFF
+    -DPNG_ARM_NEON=off
+  )
 endif()
 
 
diff --git a/yabause/CMake/Packages/external_zlib.cmake b/yabause/CMake/Packages/external_zlib.cmake
index a47b6d96..0b07e7a5 100644
--- a/yabause/CMake/Packages/external_zlib.cmake
+++ b/yabause/CMake/Packages/external_zlib.cmake
@@ -49,7 +49,11 @@ elseif(IOS)
   )
 
 else()
-  set(ADDITIONAL_CMAKE_ARGS "")
+  set(ADDITIONAL_CMAKE_ARGS
+    -DCMAKE_C_COMPILER=${CMAKE_C_COMPILER}
+    -DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER}
+    -DCMAKE_SYSROOT=${CMAKE_SYSROOT}
+  )
 endif()
 
 set(ZLIB_HEADERS
diff --git a/yabause/src/nanovg/nanovg_osdcore.c b/yabause/src/nanovg/nanovg_osdcore.c
index 520c93e9..78e7d0c7 100644
--- a/yabause/src/nanovg/nanovg_osdcore.c
+++ b/yabause/src/nanovg/nanovg_osdcore.c
@@ -36,7 +36,6 @@ Foundation, Inc., 51 Franklin Street, Fifth Floor, Boston, MA 02110-1301  USA
 
 #if defined(_OGLES3_)
 #include <GLES3/gl3.h>
-#include <GLES3/gl3ext.h>
 #else
 #include "ygl.h"
 #endif
diff --git a/yabause/src/nx/main.cpp b/yabause/src/nx/main.cpp
index c19474f0..62efa651 100644
--- a/yabause/src/nx/main.cpp
+++ b/yabause/src/nx/main.cpp
@@ -188,7 +188,7 @@ OSD_struct *OSDCoreList[] = {
 static SDL_Window* wnd;
 static SDL_GLContext glc;
 int g_EnagleFPS = 1;
-int g_resolution_mode = 0;
+int g_resolution_mode = 3;
 int g_keep_aspect_rate = 0;
 int g_scsp_sync = 1;
 int g_frame_skip = 0;
diff --git a/yabause/src/retro_arena/InputManager.cpp b/yabause/src/retro_arena/InputManager.cpp
index bd806736..20d672c5 100644
--- a/yabause/src/retro_arena/InputManager.cpp
+++ b/yabause/src/retro_arena/InputManager.cpp
@@ -213,7 +213,7 @@ int mapKeys( const json & configs ){
 
   if( SDL_NumJoysticks() == 0 ){
 
-    PADLOG("No joy stic is found force to keyboard\n");
+    PADLOG("No joystick is found force to keyboard\n");
     padbits = PerPadAdd(&PORTDATA1);
     if( configs.find("player1") == configs.end() ){
       return setDefalutSettings(padbits);
@@ -460,22 +460,22 @@ int getPlayerJsonFromInputConfig( int joy, InputConfig * inputconfig, json & pla
   }
 
   Input result;
-  inputconfig->getInputByName("a", &result);
+  inputconfig->getInputByName("y", &result);
   player[guid]["a"] ={ { "type", input_types[result.type] },{ "id", result.id },{ "value", result.value } };
 
   inputconfig->getInputByName("b", &result);
   player[guid]["b"] = { { "type", input_types[result.type] },{ "id", result.id },{ "value", result.value } };
 
-  inputconfig->getInputByName("rightshoulder", &result);
+  inputconfig->getInputByName("a", &result);
   player[guid]["c"] = { { "type", input_types[result.type] },{ "id", result.id },{ "value", result.value } } ;
 
   inputconfig->getInputByName("x", &result);
   player[guid]["x"] = { { "type", input_types[result.type] },{ "id", result.id },{ "value", result.value } };
 
-  inputconfig->getInputByName("y", &result);
+  inputconfig->getInputByName("leftshoulder", &result);
   player[guid]["y"] = { { "type", input_types[result.type] },{ "id", result.id },{ "value", result.value } };
 
-  inputconfig->getInputByName("leftshoulder", &result);
+  inputconfig->getInputByName("rightshoulder", &result);
   player[guid]["z"] = { { "type", input_types[result.type] },{ "id", result.id },{ "value", result.value } };
 
   inputconfig->getInputByName("lefttrigger", &result);
@@ -1209,7 +1209,7 @@ void InputManager::writeDeviceConfig(InputConfig* config)
 std::string InputManager::getConfigPath()
 {
   std::string path = getenv("HOME");
-  path += "/.emulationstation/es_temporaryinput.cfg";
+  path = "/opt/yabasanshiro/input.cfg";
   return path;
 }
 
diff --git a/yabause/src/retro_arena/MenuScreen.cpp b/yabause/src/retro_arena/MenuScreen.cpp
index 33591a44..d0ba2fcc 100644
--- a/yabause/src/retro_arena/MenuScreen.cpp
+++ b/yabause/src/retro_arena/MenuScreen.cpp
@@ -19,6 +19,11 @@ Foundation, Inc., 51 Franklin Street, Fifth Floor, Boston, MA 02110-1301  USA
 */
 
 #include "MenuScreen.h"
+#include "i18n.h"
+
+// Define global language variable
+Language g_current_lang = detect_language();
+
 #include <nanogui/window.h>
 #include <nanogui/layout.h>
 #include <nanogui/label.h>
@@ -64,8 +69,8 @@ extern int g_emulated_bios;
 
 
 int MenuScreen::onShow(){
-  //setupPlayerPsuhButton( 0, player1, "Player1 Input Settings", &p1cb );
-  //setupPlayerPsuhButton( 1, player2, "Player2 Input Settings", &p2cb );
+  //setupPlayerPsuhButton( 0, player1, I18N_PLAYER1_SETTINGS, &p1cb );
+  //setupPlayerPsuhButton( 1, player2, I18N_PLAYER2_SETTINGS, &p2cb );
 }
 
 MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std::string & fname, const std::string & game )
@@ -77,7 +82,7 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
   swindow = nullptr;
   imageWindow = nullptr;
 
-  int image_pix_size_w = this->width() / 2;
+  /*int image_pix_size_w = this->width() / 2;
   int image_pix_size_h = this->height() / 2;
   imageWindow = new Window(this, "About");                                                                                                         
   imageWindow->setPosition(Vector2i(0, 0));                                                                                                                   
@@ -92,7 +97,7 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
   imageView->setFixedWidth(image_pix_size_w);
   imageView->setFixedHeight(image_pix_size_h);
   imageWindow->center();
-  //imageWindow->setModal(true);
+  //imageWindow->setModal(true);*/
 
   std::string title = "Yaba Sanshiro "+ std::string(YAB_VERSION) +" Menu";
         window = new Window(this, title);
@@ -105,22 +110,22 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
         tools->setFixedWidth(256);
 
         PlayerConfig tmp;
-        tmp.player = new PopupButton(tools, "Player1", ENTYPO_ICON_EXPORT);      
-        setupPlayerPsuhButton( 0, tmp.player, "Player1 Input Settings", &tmp.cb );
+        tmp.player = new PopupButton(tools, I18N_PLAYER1, ENTYPO_ICON_EXPORT);      
+        setupPlayerPsuhButton( 0, tmp.player, I18N_PLAYER1_SETTINGS, &tmp.cb );
         player_configs_.push_back(tmp);
-        tmp.player = new PopupButton(tools, "Player2", ENTYPO_ICON_EXPORT);      
-        setupPlayerPsuhButton( 1, tmp.player, "Player2 Input Settings", &tmp.cb );
+        tmp.player = new PopupButton(tools, I18N_PLAYER2, ENTYPO_ICON_EXPORT);      
+        setupPlayerPsuhButton( 1, tmp.player, I18N_PLAYER2_SETTINGS, &tmp.cb );
         player_configs_.push_back(tmp);
 
        
-        PopupButton * ps_config = new PopupButton(tools, "Config");
+        PopupButton * ps_config = new PopupButton(tools, I18N_CONFIG);
         ps_config->setFixedWidth(248);
         showConfigDialog(ps_config);
         ps_config->setCallback([this,ps_config]() {      
           pushActiveMenu(ps_config->popup(),ps_config); 
         });
 
-        Button *b0 = new Button(tools, "Exit");
+        Button *b0 = new Button(tools, I18N_EXIT);
         b0->setFixedWidth(248);
         b0->setCallback([this]() { 
           MENU_LOG("Exit\n"); 
@@ -129,7 +134,7 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
 			    SDL_PushEvent(quit);  
         });
 
-        Button *b1 = new Button(tools, "Reset");
+        Button *b1 = new Button(tools, I18N_RESET);
         b1->setFixedWidth(248);
         b1->setCallback([this]() { 
           MENU_LOG("Reset\n");  
@@ -141,14 +146,14 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
           SDL_PushEvent(&event);          
         });        
 
-        PopupButton * ps = new PopupButton(tools, "Save State");
+        PopupButton * ps = new PopupButton(tools, I18N_SAVE_STATE);
         ps->setFixedWidth(248);
         ps->setCallback([this,ps]() {      
           showSaveStateDialog( ps->popup());
           pushActiveMenu(ps->popup(),ps); 
         });
 
-        ps = new PopupButton(tools, "Load State");
+        ps = new PopupButton(tools, I18N_LOAD_STATE);
         ps->setFixedWidth(248);
         ps->setCallback([this,ps]() {      
           showLoadStateDialog( ps->popup());
@@ -157,12 +162,12 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
 
 
 
-        bCdTray = new Button(tools, "Open CD Tray");
+        bCdTray = new Button(tools, I18N_OPEN_CD_TRAY);
         bCdTray->setFixedWidth(248);
         bCdTray->setCallback([this]() { 
           if( this->is_cdtray_open_ ){
             MENU_LOG("Close CD Tray\n"); 
-            bCdTray->setCaption("Open CD Tray");
+            bCdTray->setCaption(I18N_OPEN_CD_TRAY);
             this->is_cdtray_open_ = false;
 
             size_t pos = current_game_path_.find_last_of("/");
@@ -181,7 +186,7 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
 
           }else{
             MENU_LOG("Open CD Tray\n"); 
-            bCdTray->setCaption("Close CD Tray");
+            bCdTray->setCaption(I18N_CLOSE_CD_TRAY);
             this->is_cdtray_open_ = true;
             SDL_Event event = {};
             event.type = open_tray_;
@@ -193,7 +198,7 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
         });
 
 
-        Button *b2 = new Button(tools, "Show/Hide FPS");
+        Button *b2 = new Button(tools, I18N_SHOW_FPS);
         b2->setFixedWidth(248);
         b2->setCallback([this]() { 
           MENU_LOG("Show/Hide FPS\n");  
@@ -205,7 +210,7 @@ MenuScreen::MenuScreen( SDL_Window* pwindow, int rwidth, int rheight, const std:
           SDL_PushEvent(&event);          
         });
 
-        Button *b3 = new Button(tools, "Enable/Disable Frame Skip");
+        Button *b3 = new Button(tools, I18N_ENABLE_FRAME_SKIP);
         b3->setFixedWidth(248);
         b3->setCallback([this]() { 
           MENU_LOG("Reset\n");  
@@ -289,14 +294,14 @@ void MenuScreen::showConfigDialog( PopupButton *parent ){
 
   Popup *popup = parent->popup();    
   popup->setLayout(new GroupLayout(4,2,2,2)); 
-  new Label(popup, "Resolution");
+  new Label(popup, I18N_RESOLUTION);
   ComboBox * cb = new ComboBox(popup);  
   vector<string> items;
-  items.push_back("Native");
-  items.push_back("4x");
-  items.push_back("2x");
-  items.push_back("Original");
-  items.push_back("720p");
+  items.push_back(I18N_NATIVE);
+  items.push_back(I18N_4X);
+  items.push_back(I18N_2X);
+  items.push_back(I18N_ORIGINAL);
+  items.push_back(I18N_720P);
   items.push_back("1080p");
 
   cb->setItems(items);
@@ -312,13 +317,13 @@ void MenuScreen::showConfigDialog( PopupButton *parent ){
     VideoSetSetting(VDP_SETTING_RESOLUTION_MODE, idx);
   });
 
-  new Label(popup, "Aspect rate");
+  new Label(popup, I18N_ASPECT_RATE);
   cb = new ComboBox(popup);  
   items.clear();
-  items.push_back("Original");
-  items.push_back("4:3");
-  items.push_back("16:9");
-  items.push_back("Full Screen");
+  items.push_back(I18N_ORIGINAL);
+  items.push_back(I18N_4_3);
+  items.push_back(I18N_16_9);
+  items.push_back(I18N_FULL_SCREEN);
   cb->setItems(items);
   cbpopup = cb->popup(); 
   cb->setCallback([this,cbpopup,cb]() {       
@@ -331,14 +336,14 @@ void MenuScreen::showConfigDialog( PopupButton *parent ){
     preference->setInt("Aspect rate",idx);
   });
 
-  new Label(popup, "Rotate screen resolution");
+  new Label(popup, I18N_ROTATE_RESOLUTION);
   cb = new ComboBox(popup);  
   items.clear();
-  items.push_back("Original");
-  items.push_back("2x");
-  items.push_back("720p");
+  items.push_back(I18N_ORIGINAL);
+  items.push_back(I18N_2X);
+  items.push_back(I18N_720P);
   items.push_back("1080p");
-  items.push_back("Native");
+  items.push_back(I18N_NATIVE);
   cb->setItems(items);
   cbpopup = cb->popup(); 
   cb->setCallback([this,cbpopup,cb]() {       
@@ -353,7 +358,7 @@ void MenuScreen::showConfigDialog( PopupButton *parent ){
   });
 
 
-  Button * ba = new Button(popup,"Use compute shader");  
+  Button * ba = new Button(popup, I18N_COMPUTE_SHADER);  
   ba->setFlags(Button::ToggleButton); 
   ba->setPushed( preference->getBool("Use compute shader",false) );
   ba->setChangeCallback([this,preference](bool state) { 
@@ -362,7 +367,7 @@ void MenuScreen::showConfigDialog( PopupButton *parent ){
   });
 
 
-  ba = new Button(popup,"Rotate screen");  
+  ba = new Button(popup, I18N_ROTATE_SCREEN);  
   ba->setFlags(Button::ToggleButton); 
   ba->setPushed( preference->getBool("Rotate screen",false) );
   ba->setChangeCallback([this,preference](bool state) { 
diff --git a/yabause/src/retro_arena/Preference.cpp b/yabause/src/retro_arena/Preference.cpp
index 1108c843..4847e075 100644
--- a/yabause/src/retro_arena/Preference.cpp
+++ b/yabause/src/retro_arena/Preference.cpp
@@ -17,7 +17,7 @@ Preference::Preference( const std::string & filename ){
   }
 
   std::string home_dir = getenv("HOME");
-  home_dir += "/.yabasanshiro/";
+  home_dir = "/opt/yabasanshiro/";
 
   this->filename = home_dir + input_trace_filename + ".config";
 
diff --git a/yabause/src/retro_arena/main.cpp b/yabause/src/retro_arena/main.cpp
index a6b0c609..4a832be8 100644
--- a/yabause/src/retro_arena/main.cpp
+++ b/yabause/src/retro_arena/main.cpp
@@ -286,14 +286,19 @@ int main(int argc, char** argv)
 
   // Inisialize home directory
   std::string home_dir = getenv("HOME");
-  home_dir += "/.yabasanshiro/";
+  std::string home_dir2 = getenv("HOME");
+  home_dir = "/opt/yabasanshiro/";
+  home_dir2 = "/roms2/saturn/yabasanshiro/";
   struct stat st = {0};
   if (stat(home_dir.c_str(), &st) == -1) {
     mkdir(home_dir.c_str(), 0700);
   }  
-  std::string bckup_dir = home_dir + "backup.bin";
+  if (stat(home_dir2.c_str(), &st) == -1) {
+    home_dir2 = "/roms/saturn/yabasanshiro/";
+  }  
+  std::string bckup_dir = home_dir2 + "backup.bin";
   strcpy( buppath, bckup_dir.c_str() );
-  strcpy( s_savepath, home_dir.c_str() );
+  strcpy( s_savepath, home_dir2.c_str() );
   g_keymap_filename = home_dir + "keymapv2.json";
 
   std::string current_exec_name = argv[0]; // Name of the current exec program
@@ -597,7 +602,8 @@ int main(int argc, char** argv)
         if( ret == 0 ){
           char pngname[256];
           snprintf(pngname,256,"%s/%s_%d.png", s_savepath, cdip->itemnum, e.user.code);
-          fs::copy(tmpfilename, pngname, fs::copy_options::overwrite_existing );
+          //fs::copy(tmpfilename, pngname, fs::copy_options::overwrite_existing );
+          system((std::string("cp -f ") + tmpfilename + " " + pngname).c_str()); 
         }
         hideMenuScreen();
       }
diff --git a/yabause/src/retro_arena/nanogui-sdl/CMakeLists.txt b/yabause/src/retro_arena/nanogui-sdl/CMakeLists.txt
index 33b8e251..70ad485e 100644
--- a/yabause/src/retro_arena/nanogui-sdl/CMakeLists.txt
+++ b/yabause/src/retro_arena/nanogui-sdl/CMakeLists.txt
@@ -117,8 +117,14 @@ endif()
 include_directories(ext ext/eigen include ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})
 
 # Run simple C converter to put font files into the data segment
-add_executable(bin2c resources/bin2c.c)
-set(bin2c_cmdline nanogui_resources.cpp nanogui_resources.h)
+if(CMAKE_CROSSCOMPILING)
+  # When cross-compiling, use pre-built bin2c for host
+  set(bin2c_cmdline ${CMAKE_CURRENT_SOURCE_DIR}/bin2c)
+else()
+  add_executable(bin2c resources/bin2c.c)
+  set(bin2c_cmdline ./bin2c)
+endif()
+set(bin2c_cmdline ${bin2c_cmdline} nanogui_resources.cpp nanogui_resources.h)
 file(GLOB resources "${CMAKE_CURRENT_SOURCE_DIR}/resources/*.ttf")
 foreach(file ${resources})
   list(APPEND bin2c_cmdline ${file})
@@ -127,8 +133,8 @@ endforeach()
 # Run bin2c on resource files
 add_custom_command(
   OUTPUT nanogui_resources.cpp nanogui_resources.h
-  COMMAND ./bin2c ARGS ${bin2c_cmdline}
-  DEPENDS bin2c ${resources}
+  COMMAND ${bin2c_cmdline}
+  DEPENDS ${resources}
   COMMENT "Running bin2c"
   PRE_BUILD VERBATIM)
 
diff --git a/yabause/src/retro_arena/nanogui-sdl/src/theme.cpp b/yabause/src/retro_arena/nanogui-sdl/src/theme.cpp
index 3d126ad2..36d19834 100644
--- a/yabause/src/retro_arena/nanogui-sdl/src/theme.cpp
+++ b/yabause/src/retro_arena/nanogui-sdl/src/theme.cpp
@@ -14,6 +14,9 @@
 #include <nanogui/theme.h>
 #include <nanogui/opengl.h>
 #include <nanogui_resources.h>
+#include <cstring>
+#include <unistd.h>
+#include <libgen.h>
 
 NAMESPACE_BEGIN(nanogui)
 
@@ -72,6 +75,26 @@ Theme::Theme(NVGcontext *ctx) {
                                   entypo_ttf_size, 0);
     if (mFontNormal == -1 || mFontBold == -1 || mFontIcons == -1)
         throw std::runtime_error("Could not load fonts!");
+
+    // Load CJK font as fallback for Chinese/Japanese/Korean support
+    int cjkFont = -1;
+    char exe_path[1024] = {0};
+    char font_path[1024] = {0};
+    ssize_t len = readlink("/proc/self/exe", exe_path, sizeof(exe_path)-1);
+    if (len != -1) {
+        exe_path[len] = '\0';
+        char *dir = dirname(exe_path);
+        snprintf(font_path, sizeof(font_path), "%s/wqy-microhei.ttf", dir);
+    }
+    
+    if (font_path[0] != '\0') {
+        cjkFont = nvgCreateFont(ctx, "cjk", font_path);
+    }
+    
+    if (cjkFont != -1) {
+        nvgAddFallbackFont(ctx, "sans", "cjk");
+        nvgAddFallbackFont(ctx, "sans-bold", "cjk");
+    }
 }
 
 NAMESPACE_END(nanogui)
diff --git a/yabause/src/sdl/main.c b/yabause/src/sdl/main.c
index b9214bf6..52f539d6 100644
--- a/yabause/src/sdl/main.c
+++ b/yabause/src/sdl/main.c
@@ -141,7 +141,7 @@ NULL
 #endif
 
 static int fullscreen = 0;
-static int lowres_mode = 0;
+static int lowres_mode = 1;
 
 static char biospath[256] = "\0";
 static char cdpath[256] = "\0";
diff --git a/yabause/src/threads.h b/yabause/src/threads.h
index 45d3ef49..3c8af715 100644
--- a/yabause/src/threads.h
+++ b/yabause/src/threads.h
@@ -21,6 +21,9 @@
 #ifndef THREADS_H
 #define THREADS_H
 
+#include <stdint.h>
+typedef uint64_t u64;
+
 #ifdef __cplusplus
 extern "C" {
 #endif
@@ -115,6 +118,8 @@ void YabThreadUSleep( unsigned int stime );
 
 int YabCopyFile(const char * src, const char * dst);
 int YabMakeCleanDir( const char * dirname );
+
+#include <core.h>
 int YabNanosleep(u64 ns);
 
 ///////////////////////////////////////////////////////////////////////////
diff --git a/yabause/src/vidogl.c b/yabause/src/vidogl.c
index fe6bcc5e..717a1219 100644
--- a/yabause/src/vidogl.c
+++ b/yabause/src/vidogl.c
@@ -4373,13 +4373,19 @@ static void SetSaturnResolution(int width, int height)
 
         if (_Ygl->rotate_screen) {
           if (_Ygl->isFullScreen) {
-            if (GlHeight > GlWidth) {
-              _Ygl->originy = (GlHeight - GlWidth  * wrate);
-              GlHeight = _Ygl->screen_width * wrate;
+            // Calculate both options and choose the one that fits
+            float newHeight = GlWidth * wrate;
+            float newWidth = GlHeight * hrate;
+            
+            if (newWidth <= GlWidth) {
+              // Width fits, use width-constrained scaling
+              GlWidth = newWidth;
+              _Ygl->originx = (_Ygl->screen_width - GlWidth) / 2.0f;
             }
             else {
-              _Ygl->originx = (GlWidth - GlHeight * hrate) / 2.0f;
-              GlWidth = GlHeight * hrate;
+              // Width doesn't fit, use height-constrained scaling
+              GlHeight = newHeight;
+              _Ygl->originy = (_Ygl->screen_height - GlHeight) / 2.0f;
             }
           }
           else {
@@ -4389,13 +4395,19 @@ static void SetSaturnResolution(int width, int height)
         }
         else {
           if (_Ygl->isFullScreen) {
-            if (GlHeight > GlWidth) {
-              _Ygl->originy = (GlHeight - GlWidth  * hrate);
-              GlHeight = _Ygl->screen_width * hrate;
+            // Calculate both options and choose the one that fits
+            float newHeight = GlWidth * hrate;
+            float newWidth = GlHeight * wrate;
+            
+            if (newWidth <= GlWidth) {
+              // Width fits, use width-constrained scaling
+              GlWidth = newWidth;
+              _Ygl->originx = (_Ygl->screen_width - GlWidth) / 2.0f;
             }
             else {
-              _Ygl->originx = (GlWidth - GlHeight * wrate) / 2.0f;
-              GlWidth = GlHeight * wrate;
+              // Width doesn't fit, use height-constrained scaling
+              GlHeight = newHeight;
+              _Ygl->originy = (_Ygl->screen_height - GlHeight) / 2.0f;
             }
           }
           else {
diff --git a/yabause/src/ygl.h b/yabause/src/ygl.h
index f674228d..bfa42b0f 100644
--- a/yabause/src/ygl.h
+++ b/yabause/src/ygl.h
@@ -50,7 +50,6 @@ extern "C" {
     #include <glsm/glsm.h>
 #elif defined(__ANDROID__)
     #include <GLES3/gl31.h>
-    #include <GLES3/gl3ext.h>
     #include <EGL/egl.h>
 
 
